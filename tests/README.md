# General tests

This directory contains fixtures and tests for the shared UMAP machinery.
Stock-domain regression tests are under `../stk-specific/` and are named
`stock-*-tests.lisp`. Run individual test files from the project root using
the dependency-free test runner documented in `../README.md`.
