# General SMC implementation

This is preserved general SMC infrastructure. The stock experiment uses the
newer `../awrs-smc/` engine through stock-prefixed adapters. Run these general
tests independently with `sbcl --script smc/tests.lisp`.
