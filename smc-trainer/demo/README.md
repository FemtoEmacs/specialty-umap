# General insertion demo

`build-demo.lisp` is the copied domain-independent demonstration builder. The
stock-specific, reproducible insertion page is built by
`../../stk-specific/stock-build-transformer-insertion-demo.lisp` because it
adds stock classification and tooltip information without modifying this
general program.
