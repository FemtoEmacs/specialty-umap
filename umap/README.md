# Nested general UMAP copy

This preserved directory contains general UMAP wrappers from the source
project. The stock pipeline uses the root `build-umap.lisp` and
`score-umap.lisp`; see `../README.md`.
